// This file previously contained integration tests that required running the full binary.
// These tests were removed because they were failing and required significant maintenance.
// If integration testing is needed, consider using a more robust approach that doesn't
// rely on spawning child processes and parsing their output.
